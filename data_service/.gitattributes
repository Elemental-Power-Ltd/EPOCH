elementaldb_client_meters.sql filter=lfs diff=lfs merge=lfs -text

# Force *.sh files to use LF line endings
# We need this to stop Windows clones with autocrlf=true from breaking the postgres init scripts
*.sh text eol=lf
