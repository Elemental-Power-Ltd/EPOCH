## Building the project

1. Run `setup_vcpkg.bat` to configure vcpkg
2. Launch the project in Visual Studio and build/run
   
   The first time the project is built, all external dependencies will be installed through vcpkg.
